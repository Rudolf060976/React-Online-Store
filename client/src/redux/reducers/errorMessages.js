
import globalInitialState from '../initial-state/initialState';

const { errorMessages: initialState } = globalInitialState;

const reducer = (state = initialState, action) => {

	return state;

};

export default reducer;
